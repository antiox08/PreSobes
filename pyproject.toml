[tool.mypy]
python_version = "3.12"
strict = true

[[tool.mypy.overrides]]
module = "playwright.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
addopts = "-q -ra"
markers = [
    "smoke",
    "ui",
    "api",
]
